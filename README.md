## AI-Assisted 2D to 3D Walkthrough Backend

- Offline FastAPI backend
- Converts 2D floor plans into navigable layout
- Supports route planning (A → B)

### Run
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload

Updated backend structure (latest version)